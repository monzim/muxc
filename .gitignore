# Top-level build outputs only — not test-fixture directories like internal/cli/testdata/bin/.
/bin/
/dist/
/coverage/
*.test
coverage.out

# Local goreleaser artifacts when running `goreleaser release --snapshot` outside dist/.
/muxc_*.tar.gz
/muxc_*.deb
/muxc_*.rpm
/muxc_*_checksums.txt

# Editor / OS noise
.vscode/
.idea/
.DS_Store
